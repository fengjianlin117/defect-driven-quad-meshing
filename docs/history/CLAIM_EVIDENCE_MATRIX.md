# 论文主张—证据—缺口（2026-09-20，持续更新）

本项目 goal 已激活；本文件是过程审查，不代表投稿条件已满足。旧证据根为 `research_runs/2026-09-20_cad_decision_v2`；新证据根为 `research_runs/2026-09-20_paper_stage_v1`。以下12例均为已见开发数据，当前没有新注册的未见测试集。

| 待审查主张 | 已有依据 | 当前判断 | 下一项决定性检查 |
|---|---|---|---|
| 根据输出几何缺陷提出必要约束，而非固定比例 | v3选线与完整12例结果；B34无需约束 | 行为已实现，必要性阈值尚未标定；并非最小约束集证明 | 0.05/0.1/0.2h和10/15/20度敏感性，冻结后验证 |
| 检查硬等式能避免固定坐标框架下的压扁 | B49、B53、B31的暂缓记录；本轮逐组消减审计 | 新发现：4个暂缓组各自单独就不相容。不能将其称为组间冲突实例 | 独立路径证书、实际后端轴规则核验；区分组内/组间/整数全局冲突 |
| 组合替换改善结果 | 当前贪心逐组添加 | 尚无独立支持；4个已有冲突组不能靠替换单个其他组解决 | 在固定预算下处理局部组内冲突与有预算回退；保持全部参考 |
| 实测残余驱动尺寸调整保护未选特征 | 本轮3例、6次后端调用；B49反馈528Q通过旧保护；另2例回退 | 仅开发机制信号。B49面数增加，不能直接称等成本优势 | 同实际面数的无反馈/均匀加密对照；步长敏感性；全部12例消融 |
| 总体误差下降即保护了每条参考特征 | B31旧合计未选缺陷下降 | 不成立。逐边正退步约6.12，与合计下降同时出现 | 固定共同参考、逐边正退步及公差越界长度；禁止跨方法比较各自未选集 |
| 估计面数足够满足几何需求 | 现有源几何尺寸代理与B32数量曲线 | 没有误差界；本轮三例局部反馈仍未推高代理预算，却发生数量偏差 | 面数预测误差、密度重新分布影响、额外预算成本分列 |
| 整体有效性随加密改善 | B43 783/约989/1907Q反例；本轮B53首输出失败 | 不成立 | 保留非单调案例；上限与停止规则不能依赖单调假设 |
| 方法优于强基线及外部方法 | v2无/全特征、简单排序；无外部完整对照 | 未证实；B70全特征是强对照 | 冻结后比较QuadriFlow sharp与可运行的QuadWild/Gmsh，报告实际面数曲线 |
| 对未见CAD工件族有泛化价值 | 12例全部已见；既有MAMBO新六未做族隔离 | 未证实 | 冻结方法后注册新模型族与排除规则，不按输出挑样本 |
| 可用于工程CAD尺寸公差 | 当前误差以基准h归一化，输入是三角表面 | 未证实；不是CAD公差认证 | 源单位/精确CAD身份与关键尺寸、绝对及尺度归一化公差；分清离散源误差 |
| 全局无自交、高质量结构保证 | 当前拓扑与双线性局部秩检查 | 不成立，基本通过仅表示已有检查通过 | 增补独立全局相交检查与方向检查，保留局部退化和投影质量警告 |
| 投稿证据包齐全 | 旧归档可核验；新审计与首轮机制实验可追溯 | 未完成 | 完整方法、外部对照、独立测试、稳定性、图表、论文全文与重放 |

## 首轮新发现

1. 重新逐文件计算旧4613项SHA256，缺失0、变更0；仓库现有未提交修改已另存状态和跟踪文件散列。
2. 对B49 geometry-0002、B53 geometry-0001、B31 geometry-0008/0009逐组消减，得到单组即可触发的固定框架压扁。单个其他组替换无可行候选。解释边界限于基线cut及combed轴，不推广到其他场、cut或软约束。
3. B31 allocated_0：旧未选合计变化为-1.7167，但同集合逐边正退步合计为6.1235。已保存每条边与组的归属、前后距离/方向诊断和相邻要求尺寸。
4. 本轮尺寸反馈先冻结3例全部输入；相同代理预算与按需求估计预算的两臂在这3例中恰好完全一致。输出前增加执行去重说明，每个相同输入只求解一次，不计为两份证据。

## 不能省略的结果

原保护规则尚未放宽。B49反馈首次388Q、缺陷12.5417，数量校正528Q、缺陷11.1532；后者通过旧保护。B53首次440Q基本检查失败，校正512Q通过基本检查但未通过保护；B65两次均基本通过但未通过保护。任何候选及回退都不能替代未见测试。

精确数字与路径以新证据根 `localization/*.json`、`feedback_pilot_v1/runs/summary.json`、各 `result.json` 与 `backend_run.json` 为准。
