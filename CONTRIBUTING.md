# 协作约定

## 开始工作

先读 README、docs/current_status.md 和与你修改部分对应的结果报告。默认分支为 main。使用 feature/<topic>、fix/<topic>、docs/<topic> 等个人分支；工具生成的分支可用 codex/<topic>。无写权限的协作者通过 fork 提交 PR，不需要交换账号或密钥。

## 代码与实验

- 当前默认控制流程为 conditional_coordinator；coordinator_v5 是持续反馈及消融使用的基础版本。新实验必须写明选择哪一种。
- Python 方法及基础依赖在 method；不要把旧 structural_graph 或其他历史入口重新当作必要依赖引入。
- 输出写入新的 outputs/<experiment-id>，不要覆盖 experiments/inputs、已有 results/records 和历史候选。
- 修改阈值、停止规则、评价或输入后，使用新的实验标识与协议；保持旧结果可解释。
- 12个已有模型为开发集；不得把换目录、缓存重放或同模型多预算计成新验证模型。
- source.obj 与方向场逐面索引必须保持对应；不得悄悄重新排序面或顶点。
- 先运行 `python -m pytest`、`python scripts/verify_snapshot.py`。若修改了冻结材料，先说明原因并审核新结果，再显式更新发布清单；不要为让验证通过而直接刷新散列。

## 一个实验至少记录

代码 commit、方法版本、输入及场的 SHA256、模型/族划分、所有参数、种子、后端与依赖版本、停止预算、目标与实际四边形数、质量/特征指标、接受/回退理由、新求解与精确复用次数、时间统计范围。失败和超时也应保留。

## Pull request

写清问题、改后行为、验证命令与结果、受影响的论文结论。涉及方法改动时，提供一项有意义的机制检查或对照，不仅汇报总成功数。文档和结果表中的定量结论应链接到可核查文件。

论文工作稿在 paper。修改已有数字时，附上来源记录；新增实验的结果与历史版本分开。不要把后端复用、训练开销、CPU前处理和整体耗时混为一项。

新运行的大量中间文件、环境、权重与二进制不直接提交；提交必要清单和汇总，另行约定大型归档位置。本仓库当前没有统一新授权条款，使用和贡献第三方内容须保留其原许可与来源。
